name="Honey  is a good girl"
print (name)
print(name.replace("  ", " "))
#the main string name is not changed because strings are immutable in Python. The replace() function returns a new string with the replaced value.
# strings are immutable in Python, which means that they cannot be changed after they are created. The replace() function returns a new string with the replaced value, but it does not change the original string.